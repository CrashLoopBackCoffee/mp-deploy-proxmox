name: deploy-proxmox
runtime:
  name: python
  options:
    toolchain: poetry
    virtualenv: .venv
description: Proxmox cluster configuration
config:
  pulumi:tags:
    value:
      pulumi:template: python
