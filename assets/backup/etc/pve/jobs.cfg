vzdump: backup-{name}
        schedule {schedule}
        compress zstd
        enabled 1
        fleecing 0
        mode snapshot
        notes-template {{{{guestname}}}}
        notification-mode notification-system
        prune-backups {prune}
        storage {storage}
        vmid {vmid}
